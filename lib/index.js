/*
Sparky already has tokens intergrated. All the tokens are in the variable token. To skip check for all the tokens you can use token[i].
*/
if (token[i][0] === "testCheck") {
    // Do something...
}